# Database migrations

Alembic migrations will live here. The database schema is intentionally not
created from application startup; every schema change must be versioned and
applied explicitly.

Initial migration work will add organization, user, catalog, rental unit,
pricing, reservation, order, fulfillment, deposit, and invoice tables.
